name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
      cd rocket
      cargo build --verbose
    - name: Run tests
      run: |
      cd rocket
      cargo test --verbose
